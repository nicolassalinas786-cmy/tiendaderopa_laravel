# 🎨 Archivo: `public/css/`

- **Ruta real:** [`public/css/`](file:///c:/laragon/www/tienda-ropa/public/css/)
- **Función:** Modularización y Desacoplamiento de Estilos CSS.
- **Descripción:**
  Contiene los estilos CSS organizados independientemente del código HTML y Blade:
  1. [`app.css`](file:///c:/laragon/www/tienda-ropa/public/css/app.css): Tipografía general 'Inter', comportamiento de desplazamiento suave (`scroll-behavior: smooth`) y animaciones SVG para confirmación de pedidos (`drawCheck`).
  2. [`auth.css`](file:///c:/laragon/www/tienda-ropa/public/css/auth.css): Diseño visual para los formularios de login y registro (`.step-line`, `.input-box`, transiciones).
  3. [`admin.css`](file:///c:/laragon/www/tienda-ropa/public/css/admin.css): Estilos para la barra lateral y tarjetas del panel de administración (`.nav-item`, `.nav-item:hover`, `.nav-item.active`, `.stat-card`).
