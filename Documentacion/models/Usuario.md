# 👤 Archivo: `app/Models/Usuario.php`

- **Ruta real:** [`app/Models/Usuario.php`](file:///c:/laragon/www/tienda-ropa/app/Models/Usuario.php)
- **Función:** Modelo Eloquent de Usuario y Autenticación.
- **Descripción:** Extiende de `Illuminate\Foundation\Auth\User`, gestiona el acceso seguro a la tabla `usuarios`, define la clave primaria personalizada `id_usuario`, mapea el campo `correo` como identificador de inicio de sesión y provee métodos auxiliares como `esAdmin()`, `getNombreCompletoAttribute()` y `getInitialAttribute()`.
